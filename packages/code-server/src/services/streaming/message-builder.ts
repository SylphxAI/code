/**
 * Message Builder
 * Re-exports from @sylphx/code-core for backward compatibility
 *
 * The buildModelMessages function has been moved to code-core
 * since it's used by both streaming service and compact service.
 */

export { buildModelMessages } from '@sylphx/code-core';
